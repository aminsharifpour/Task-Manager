PORT=8787
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=12h
# Optional. Example: http://localhost:5173,https://app.example.com
# CORS_ORIGIN=
# Optional first-run bootstrap admin (only used when no admin exists)
# TASK_APP_DEFAULT_ADMIN_PHONE=09120000000
# TASK_APP_DEFAULT_ADMIN_PASSWORD=ChangeThisStrongPassword
# اگر فرانت و بک‌اند روی دامنه جدا هستند:
# VITE_API_BASE=https://your-api-domain.com
